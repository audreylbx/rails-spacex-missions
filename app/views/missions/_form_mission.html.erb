<%= simple_form_for [@planet, @mission] do |f| %>
  <%= f.input :description, label: 'Mission', placeholder: 'Send rations to survivors' %>
  <%= f.association :spaceshift, label: 'Spaceship' %>
  <%= f.button :submit, 'Update' %>
<% end %>
