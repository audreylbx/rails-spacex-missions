<%= simple_form_for [@planet, @mission] do |f| %>
  <%= f.input :description, label: 'Mission', placeholder: 'Send rations' %>
  <%= f.association :spaceshift %>
  <%= f.button :submit, 'Update' %>
<% end %>
